Use Case: Use equation in symbolic algebra system
Actor(s): User
Purpose: Take an equation on a web site or in a word processing
document and import into a symbolic algebra system
-------------------------
Description: 

User views web site with MathML expression and wishes to verify the
accuracy of a derivation. User starts up a symbolic algebra system and 
copies the expression from the web site to the system's console.

Alternative courses:

1. User sees the equation in a word processing document rather than on 
a web page.

2. User takes equation from symbolic algebra system and copies it to
a word processing document he is editing.

-------------------------
Type: primary, domain oriented

Cross References: 
Embedding into word processor documents
Use as the renderer in a symbolic algebra system
