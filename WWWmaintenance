When updating the website, please also make an up-to-date version of the html
documentation:  in a linbox installation, 
make the html docs and copy the html directory to here.

1. cd <path>/linbox ; git checkout master

2. (cd doc; make docs)

3. cd <path>/www-linbox ; git checkout gh-pages

4. rsync --verbose --update --recursive --links --times <path>/linbox/doc/linbox-html . 

5. git add linbox-html ; git commit -a ; git pull ; git push
